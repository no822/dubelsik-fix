{
  "name": "dubelsik-converter",
  "version": "0.1.0",
  "private": true,
  "description": "Two-set (두벌식) keyboard transliterator from alphabet input to composed Hangul.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "dubelsik": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "dev": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  }
}
